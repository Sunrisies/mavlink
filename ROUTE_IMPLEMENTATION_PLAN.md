# Rust 航线功能实现计划

本文档描述在本项目中使用 Rust 重新实现航线功能的分步计划。C/C++ 项目只作为协议和业务需求参考，不直接迁移 C/C++ 代码。

## 第 1 步：定义 Rust 航线数据模型

完成：

- 定义航线结构、航点结构和航线名称；
- 明确航点编号规则；
- 明确经纬度、高度、停留时间等字段的类型和单位；
- 区分业务航点和 MAVLink 任务项。

建议的概念结构：

```text
Route
├── name
└── waypoints

Waypoint
├── seq
├── latitude
├── longitude
├── altitude
└── 可选业务字段
```

本步骤只定义数据，不连接飞控。

## 第 2 步：定义 Rust 航线状态机

完成：

- 定义航线当前操作状态；
- 防止读取和写入同时进行；
- 处理重复请求；
- 记录上传和下载进度；
- 支持失败、超时和取消。

状态至少包括：

```text
Idle
Uploading
WaitingUploadAck
RequestingList
Downloading
Clearing
Failed
```

## 第 3 步：实现 Rust 到 MAVLink 的上传

完成：

- 接收 Rust 内部的航线数据；
- 发送 `MISSION_COUNT`；
- 响应 `MISSION_REQUEST` 和 `MISSION_REQUEST_INT`；
- 发送 `MISSION_ITEM_INT`；
- 等待 `MISSION_ACK`；
- 发布上传进度、成功和失败结果。

数据方向：

```text
Rust 航线数据 -> MAVLink -> 飞控
```

## 第 4 步：实现 MAVLink 到 Rust 的下载

完成：

- 发送 `MISSION_REQUEST_LIST`；
- 接收 `MISSION_COUNT`；
- 按序请求并接收每个任务项；
- 转换为 Rust 航点；
- 校验序号、数量和重复消息；
- 下载完成后生成完整航线。

数据方向：

```text
飞控 MAVLink 任务 -> Rust 航线数据
```

不要根据经纬度是否为 `0,0` 判断任务类型，应根据任务项的序号、命令和协议约定处理。

## 第 5 步：处理 Home 项和特殊任务项

完成：

- 确认飞控任务列表是否包含 Home 项；
- 区分 Home 项、普通航点和其他 MAVLink 任务项；
- 明确哪些项目返回给前端；
- 保留原始 MAVLink 序号与业务序号之间的转换关系。

不能简单通过坐标值过滤任务项。

## 第 6 步：实现清除飞控航线

完成：

- 发送 `MISSION_CLEAR_ALL`；
- 等待 `MISSION_ACK`；
- 返回成功或失败结果；
- 保证清除与上传、读取操作互斥。

清除语义必须分开：

```text
网页清除：只清除前端显示，不操作飞控
飞控清除：发送 MISSION_CLEAR_ALL，清除飞控任务
```

## 第 7 步：实现 MQTT 航线接口

`set_list`：接收名称和航点列表，启动上传，发布上传进度及结果。

`get_list`：启动下载，发布读取开始、单个航点和完整读取结果。

`clear_list`：启动飞控清除，发布清除结果。

空的 `set_list` 不应承担清除飞控航线的语义，清除应使用明确的 `clear_list` 操作。

## 第 8 步：扩展航点业务字段

在基础坐标航线稳定后，再逐步增加：

- 航点名称；
- 停留时间；
- 速度；
- 航点模式；
- 取水模式、取水瓶号和取水时间。

可以映射到 MAVLink 的字段直接转换；不能放入标准 MAVLink 任务项的字段由 Rust 服务端保存，并通过业务协议传递。所有时间字段必须明确单位。

## 第 9 步：实现航线名称管理

完成：

- 接收和返回航线名称；
- 将名称与航点列表关联；
- 读取航线时返回名称；
- 明确服务重启后名称是否保留。

第一版可以保存在 Rust 进程内存中，后续再选择 JSON 文件、SQLite 或数据库持久化。

## 第 10 步：增加自动化测试

覆盖：

- 空、单个和多个航点的上传；
- `MISSION_REQUEST` 和 `MISSION_REQUEST_INT`；
- 空航线和多航点下载；
- Home 项处理；
- 航点乱序和重复；
- 上传期间收到读取请求；
- 清除期间收到上传请求；
- 飞控失败 ACK；
- 网页清除不发送飞控清除命令。

优先使用模拟 MAVLink 连接测试状态机和协议流程，再使用真实飞控进行集成验证。

## 推荐实施顺序

```text
1. Rust 数据模型
2. Rust 状态机
3. MAVLink 上传
4. MAVLink 下载
5. Home/特殊任务项处理
6. MAVLink 清除
7. MQTT 接口
8. 航点业务字段
9. 航线名称管理
10. 自动化测试
```

每一步完成并验证后，再进入下一步，避免同时修改前端、MQTT、状态机和 MAVLink 协议。
